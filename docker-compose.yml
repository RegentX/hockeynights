services:
  frontend:
    image: ${IMAGE:-ghcr.io/hockeynights/hockeynights-frontend:latest}
    container_name: hockeynights-frontend
    ports:
      - "${APP_PORT:-8081}:80"
    restart: unless-stopped
