# Fail on peer dependency conflicts (default, documented explicitly).
# Do not use legacy-peer-deps — it hides incompatibilities until runtime/CI.
legacy-peer-deps=false

# Enforce engines.node from package.json
engine-strict=true
